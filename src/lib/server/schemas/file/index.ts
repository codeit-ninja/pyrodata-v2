import type { createFileSchema } from "./schema";
import { db } from "$lib/db";

/**
 * TODO: Implement method
 */
export const createFile = async (data: createFileSchema) => {
    
}

export * from './schema';